# 这个项目在 Windows 上开发、部署到 Linux。
# 必须强制 LF，否则 deploy/*.sh 拉到服务器上会因为 \r 直接跑不起来
# （报错长这样：/usr/bin/env: 'bash\r': No such file or directory）。
* text=auto eol=lf
*.sh   text eol=lf
*.js   text eol=lf
*.mjs  text eol=lf
*.json text eol=lf
*.md   text eol=lf
*.yml  text eol=lf
*.html text eol=lf
*.css  text eol=lf

# 这些是二进制/不该被改动的东西
*.png binary
*.pem binary
*.ico binary
